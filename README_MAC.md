# 🚀 AutoPilot PulseChain Bot – Instructions macOS

Bienvenue dans la version macOS du bot AutoPilot PulseChain.
Comme les fichiers `.command` ne sont pas exécutables par défaut,
vous devez activer le lanceur une seule fois avant de pouvoir double-cliquer dessus.

---

## 📌 1) Ouvrir le Terminal

Sur votre Mac, ouvrez l’app **Terminal**.

---

## 📌 2) Naviguer vers le dossier des lanceurs Mac

Tapez la commande suivante puis faites glisser le dossier `launcher_mac` dans la fenêtre du Terminal :

```bash
cd 
